# Pinned to what is actually installed and verified working on
# Python 3.13 / Windows with CPU-only PyTorch.
#
# torch comes from the CPU wheel index:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#
# The chat LLM is NOT a pip package: it is served by Ollama.
#   ollama pull gemma4:12b

fastapi==0.141.1
uvicorn==0.53.0
timesfm==3.0.2
torch==2.14.0
huggingface-hub==1.2.1
yfinance==1.7.0
pandas==3.0.6
numpy==2.5.3
SQLAlchemy==2.0.54
python-dotenv==1.2.3
requests==2.34.2
APScheduler==3.11.3
